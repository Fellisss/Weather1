@model Weather1.Models.Observation
@{
    ViewData["Title"] = "Удалить наблюдение";
}

<h2>Удалить наблюдение</h2>

<div>
    <h4>Вы уверены, что хотите удалить это наблюдение?</h4>
    <dl class="row">
        <dt class="col-sm-2">Город</dt>
        <dd class="col-sm-10">@Model.City</dd>

        <dt class="col-sm-2">Время</dt>
        <dd class="col-sm-10">@Model.Timestamp</dd>

        <dt class="col-sm-2">Температура</dt>
        <dd class="col-sm-10">@Model.Temperature °C</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Удалить</button>
        <a asp-action="Archive" class="btn btn-secondary">Отмена</a>
    </form>
</div>